/** Autocomplete hints — only `email` fields should allow address autofill. */
export const AC = {
  email: "email",
  off: "off",
  otp: "one-time-code",
  newPassword: "new-password",
  currentPassword: "current-password",
} as const;
